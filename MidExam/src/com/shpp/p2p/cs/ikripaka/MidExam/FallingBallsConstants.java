package com.shpp.p2p.cs.ikripaka.MidExam;

public interface FallingBallsConstants {
    /**
     * Ball radius
     */
    public static final int BALL_RADIUS = 30;

    public static final double BALL_SIZE = 50;

    /* The amount of time to pause between frames (48fps). */
    public static final double PAUSE_TIME = 1000.0 / 48;

    /* The initial horizontal velocity of the ball. */
    public static final double HORIZONTAL_VELOCITY = 1.0;

    /* Gravitational acceleration. */
    public static final double GRAVITY = 0.425;

    /* Elasticity. */
    public static final double ELASTICITY = 0.75;
}
